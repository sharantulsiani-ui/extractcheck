# Suggested GitHub topics

Use these topics for the ExtractCheck repository if it is published:

`document-parsing` `document-extraction` `document-ai` `rag` `pdf` `pptx`
`xlsx` `docx` `benchmark` `provenance` `offline-ai` `information-retrieval`
`synthetic-data` `privacy`

Suggested repository description:

> An offline benchmark for testing document extraction across PDF, PowerPoint
> and Excel, with exact source provenance.

These are discovery terms, not claims that ExtractCheck is a universal parser,
an air gap or a complete RAG evaluation.
